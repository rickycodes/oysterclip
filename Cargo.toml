[package]
name = "clipboard-watcher"
version = "0.2.0"
edition = "2021"


[dependencies]
clap = { version = "4", features = ["derive"] }
arboard = "2"
humantime = "2"
image = "0.25" # used for PNG encoding